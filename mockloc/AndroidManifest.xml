<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pikminbot.mockloc"
    android:versionCode="11"
    android:versionName="1.9">

    <uses-sdk android:minSdkVersion="21" android:targetSdkVersion="34" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_MOCK_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:label="@string/app_name"
        android:icon="@android:drawable/ic_menu_compass"
        android:theme="@android:style/Theme.DeviceDefault.Light">

        <!-- Self-contained UI: enter coords, tap Start. No computer needed. -->
        <activity android:name=".MainActivity"
                  android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground service: pins the mock GPS fix with a 1s loop so it
             never goes stale on Android 12+. Started from MainActivity,
             the adb broadcast receiver below, or directly via
             `adb shell am start-foreground-service -n com.pikminbot.mockloc/.MockService -ef lat X -ef lon Y`.
             exported=true lets the shell start it without launching the UI
             (safe: inject() still fails unless THIS app holds the
             MOCK_LOCATION appop, so other packages can't abuse it). -->
        <service android:name=".MockService"
                 android:exported="true"
                 android:foregroundServiceType="location" />

        <!-- Adb-driven entry (pbgw bridge). Delegates to MockService. -->
        <receiver android:name=".MockLocationReceiver"
                  android:exported="true">
            <intent-filter>
                <action android:name="com.pikminbot.mock.SET_LOCATION" />
                <action android:name="com.pikminbot.mock.CLEAR_LOCATION" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
