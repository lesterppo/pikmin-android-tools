# Build artifacts (regenerated locally) — but KEEP published release APKs
build/
.gradle/
.kotlin/
local.properties
*.jks
*.keystore
*.idsig
# ignore apks everywhere EXCEPT the published releases/ folder
*.apk
!releases/
!releases/*.apk
